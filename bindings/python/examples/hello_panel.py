"""Compatibility launcher for the component gallery.

This file intentionally stays small so it can become a simpler hello-panel
example later without moving the gallery again.
"""

from component_gallery import build_view, main


if __name__ == "__main__":
    main()
